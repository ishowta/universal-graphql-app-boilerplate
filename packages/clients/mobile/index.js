/**
 * @format
 */

import {AppRegistry} from '@app/app/node_modules/react-native';
import App from '@app/app/src/App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => App);
