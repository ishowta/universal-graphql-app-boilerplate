{
  "name": "@app/db",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": ". ../../scripts/loadenv.sh && psql -U $DB_ROOT_USER -d postgres -f ./reset.pgsql && psql -U $DB_ROOT_USER -d $DATABASE_NAME -f ./schema.pgsql && psql -U $DB_ROOT_USER -d $DATABASE_NAME -f ./constraints/*.pgsql && psql -U $DB_ROOT_USER -d $DATABASE_NAME -f ./seeds.pgsql",
    "reset": "yarn setup"
  },
  "dependencies": {},
  "devDependencies": {}
}
