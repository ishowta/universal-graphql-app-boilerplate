{
  "name": "@app/db",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": ". ../../scripts/loadenv.sh && psql -U postgraphile -d $DATABASE_NAME -f ./setup.pgsql && psql -U postgraphile -d $DATABASE_NAME -f ./schema.pgsql && psql -U postgraphile -d $DATABASE_NAME -f ./controllers/*.pgsql && psql -U postgraphile -d $DATABASE_NAME -f ./seeds.pgsql",
    "reset": "yarn setup"
  }
}
